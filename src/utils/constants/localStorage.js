const TOKEN = 'token';
const USER = 'user';
const TOKEN_EXPIRED = 'tokenExpired';
const REFRESH_TOKEN = 'refreshToken';
const REFRESH_TOKEN_EXPIRED = 'refreshTokenExpired';

export default {
    TOKEN,
    USER,
    TOKEN_EXPIRED,
    REFRESH_TOKEN,
    REFRESH_TOKEN_EXPIRED,
};